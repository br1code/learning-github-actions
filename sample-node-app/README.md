# Sample Node App

This is a tiny Node.js app created for GitHub Actions learning.

## Commands

- `npm install`
- `npm run build`
- `npm test`
- `npm start`

The app calculates a small sum and prints a friendly message using a small dependency.
